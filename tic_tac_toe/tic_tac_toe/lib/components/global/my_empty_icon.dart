import 'package:flutter/material.dart';

class MyEmptyIcon extends StatelessWidget {
  const MyEmptyIcon({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("");
  }
}
